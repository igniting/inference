# Part IV — Beyond Text-Only Decoding

The same state-and-topology reasoning applied where the decoder loop does not
rule: images that flow through encoders, diffusion pipelines that iterate over
a latent, learning loops that rewrite the model underneath a live service, and
voice sessions that must answer within a human sense of time.

**Chapters 17–20**
